#include"bits/stdc++.h"
using namespace std;

#define all(x) begin(x), end(x)
#define FOR(i,a,b) for(int i=a;i<=b;i++)
const int MX = 1e5+10;	
 
struct query{
	char a;
	ll l , r; 
} q[MXN];
ll n, tree[MXN];
int main(){
	cin.tie(0); cout.tie(0) -> sync_with_stdio(0);
	
} 
